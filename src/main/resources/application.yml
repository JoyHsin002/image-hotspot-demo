spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 11MB
  jackson:
    serialization:
      write-dates-as-timestamps: false
hotspot:
  data-dir: ${HOTSPOT_DATA_DIR:data}
